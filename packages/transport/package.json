{
  "name": "@dot-protocol/transport",
  "version": "1.0.0-alpha.0",
  "description": "iroh adapter layer — bridges DOT chain semantics to iroh P2P transport (and MemoryTransport for testing).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@dot-protocol/core": "workspace:*",
    "@dot-protocol/chain": "workspace:*",
    "@dot-protocol/mesh": "workspace:*",
    "@dot-protocol/sync": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  },
  "license": "Apache-2.0"
}
